#include <stdio.h> // 预处理器指令，告诉C编译器在实际编译之前要包含stdio.h文件

int main() // 主函数，程序从这里开始执行
{
	/* 我的第一个C程序 */ // 注释，会被编译器忽略
	printf("Hello, World!\n"); // C中另一个可用的函数，会有屏幕上显示消息“Hello, World!”
	return 0; // 终止main()函数，并返回值0
}
